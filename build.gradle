plugins {
    id 'java'
}

group = 'com.wickidcow.aetherlegacy'
version = '0.1.0-SNAPSHOT'

def paperVersion = '26.2.build.112-stable'

repositories {
    mavenCentral()
    maven {
        name = 'papermc'
        url = 'https://repo.papermc.io/repository/maven-public/'
    }
}

dependencies {
    compileOnly "io.papermc.paper:paper-api:${paperVersion}"
}

java {
    toolchain.languageVersion = JavaLanguageVersion.of(25)
}

sourceSets {
    main {
        java.srcDirs = ['src/paper/java']
        resources.srcDirs = ['src/paper/resources']
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
    options.release = 25
}

processResources {
    filesMatching('plugin.yml') {
        expand(version: project.version)
    }
}

jar {
    archiveBaseName = 'AetherLegacyPaper'
    archiveVersion = project.version
    manifest {
        attributes(
            'Implementation-Title': 'Aether Legacy for Paper',
            'Implementation-Version': project.version,
            'Built-For': 'Paper 26.2 / Java 25'
        )
    }
}

tasks.named('build') {
    dependsOn tasks.named('jar')
}
